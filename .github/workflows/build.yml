on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: pguyot/arm-runner-action@v2
      with:
        commands: |
            apt-get install -y --no-install-recommends tree
            git submodule update --remote src/build
            cd src/build
            tree
            
